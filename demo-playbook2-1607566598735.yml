---
# Red Hat Insights has recommended one or more actions for you, a system administrator, to review and if you
# deem appropriate, deploy on your systems running Red Hat software. Based on the analysis, we have automatically
# generated an Ansible Playbook for you. Please review and test the recommended actions and the Playbook as
# they may contain configuration changes, updates, reboots and/or other changes to your systems. Red Hat is not
# responsible for any adverse outcomes related to these recommendations or Playbooks.
#
# Demo-Playbook2
# https://cloud.redhat.com/insights/remediations/731b53ed-c663-4230-b86e-6d1a715e46d1
# Generated by Red Hat Insights on Thu, 10 Dec 2020 02:16:38 GMT
# Created by armando.vega@javanes.com

# Upgrade the following packages:
#   - Upgrade packages affected by CVE-2018-3639
#   - Upgrade packages affected by CVE-2018-3639
#   - Upgrade packages affected by CVE-2018-3639
# Identifier: (vulnerabilities:CVE-2018-3639:CVE_2018_3639_cpu_kernel|CVE_2018_3639_CPU_BAD_CMDLINE_2,vulnerabilities:CVE-2018-3639:CVE_2018_3639_cpu_kernel|CVE_2018_3639_CPU_BAD_KERNEL_2,vulnerabilities:CVE-2018-3639:CVE_2018_3639_cpu_kernel|CVE_2018_3639_CPU_BAD_MICROCODE_2,fix)
# Version: 9fc7f2c5c272054815ae190afb31af7e3ee825ff
- name: update vulnerable packages
  hosts: "node1,node2,node3"
  become: true
  tasks:
    - name: check for update
      shell: "{{ ansible_facts['pkg_mgr'] }} check-update -q --cve CVE-2018-3639 --cve CVE-2018-3639 --cve CVE-2018-3639"
      check_mode: no
      register: check_out
      failed_when: check_out.rc != 0 and check_out.rc != 100
      args:
        warn: false

    - when: check_out.rc == 100
      name: upgrade package
      shell: "{{ ansible_facts['pkg_mgr'] }} upgrade -v -y --cve CVE-2018-3639 --cve CVE-2018-3639 --cve CVE-2018-3639"
      args:
        warn: false

    - when: check_out.rc == 100
      name: set reboot fact
      set_fact:
        insights_needs_reboot: True

# Reboots a system if any of the preceeding plays sets the 'insights_needs_reboot' variable to true.
# The variable can be overridden to suppress this behavior.
- name: Reboot system (if applicable)
  hosts: "node1,node2,node3"
  become: True
  gather_facts: False
  tasks:
    - when:
        - insights_needs_reboot is defined
        - insights_needs_reboot
      block:
        - name: Reboot system
          shell: sleep 2 && shutdown -r now "Ansible triggered reboot"
          async: 1
          poll: 0
          ignore_errors: true

        - name: Wait for system to boot up
          local_action:
            module: wait_for
            host: "{{ hostvars[inventory_hostname]['ansible_host'] | default(hostvars[inventory_hostname]['ansible_ssh_host'], true) | default(inventory_hostname, true) }}"
            port: "{{ hostvars[inventory_hostname]['ansible_port'] | default(hostvars[inventory_hostname]['ansible_ssh_port'], true) | default('22', true) }}"
            delay: 15
            search_regex: OpenSSH
            timeout: 300
          become: false

- name: run insights
  hosts: "node1,node2,node3"
  become: True
  gather_facts: False
  tasks:
    - name: run insights
      command: insights-client
      changed_when: false